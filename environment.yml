name: Koala
channels:
  - conda-forge
dependencies:
  - conda
  - python=3.9
  - ipykernel #this makes the env show up in the list of jupyter environments
  - numpy
  - scipy
  - matplotlib
  - pip
  - pytest
  - nbmake
  - flake8
  - pip:
    # works for regular pip packages
    - python-sat #the frontend to the sat solver